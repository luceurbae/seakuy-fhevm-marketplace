# Smart Contract Deployment
PRIVATE_KEY=your_wallet_private_key_here
SEPOLIA_RPC_URL=https://eth-sepolia.public.blastapi.io
ETHERSCAN_API_KEY=your_etherscan_api_key_optional

# IPFS Storage (Optional - for NFT metadata)
VITE_PINATA_JWT=your_pinata_jwt_token_here

# Development Mode
# For local testing without blockchain, set VITE_DEMO_MODE=true (RECOMMENDED)
# For blockchain testing (Sepolia testnet only), set VITE_DEMO_MODE=false
VITE_DEMO_MODE=true

# Network Mode (localhost or sepolia)
# Note: localhost requires FHEVM contracts deployed, use demo mode instead
VITE_NETWORK_MODE=localhost

# Instructions:
# 1. Copy this file to .env
# 2. For easiest local testing, keep VITE_DEMO_MODE=true
# 3. For Sepolia testnet testing, set VITE_DEMO_MODE=false and VITE_NETWORK_MODE=sepolia
# 4. Never commit .env to version control!
